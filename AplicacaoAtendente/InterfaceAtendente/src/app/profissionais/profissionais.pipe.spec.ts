import { ProfissionalPipe } from './profissionais.pipe';

describe('ProfissionalPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfissionalPipe();
    expect(pipe).toBeTruthy();
  });
});
